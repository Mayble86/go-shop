version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: goshopdb
    restart: always
    environment:
      POSTGRES_DB: goshopdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "15432:5432"
